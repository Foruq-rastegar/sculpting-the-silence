# OS junk files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Local Claude Code settings (machine-specific paths, not for sharing)
.claude/settings.local.json

# Exhibition-only assets kept locally for the offline flash-drive build,
# deliberately not tracked in the GitHub repo (see local-only-assets/README.md)
local-only-assets/
